# 贡献

- 每个演讲目录都由具体日期命名，如 `2025-11`。
- 请在演讲目录下创建 `slides.md` 文件，并编写演讲内容。该内容使用 [slidev](https://cn.sli.dev/) 直接渲染，因此请撰写符合 slidev 语法的 `markdown` 文件内容。
- 当一个分支被合并到主分支，将直接使用 `Netlify` 自动构建并部署到 `https://talks.arkcode.dev` 下，可供公开访问。访问的路径比如演讲目录名称为 `2025-11`，则访问路径为 `https://talks.arkcode.dev/2025-11`。
